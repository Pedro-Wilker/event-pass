import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.eventpasspromain',
  appName: 'event-pass-pro-main',
  webDir: 'dist'
};

export default config;
